# Manual Payment Card Encryption

## Setup

```bash
// copy example env vars to your local

copy .env.example .env.local

// Update NEXT_PUBLIC_RSA_PUBLIC_KEY to public key
NEXT_PUBLIC_RSA_PUBLIC_KEY="..."
```
